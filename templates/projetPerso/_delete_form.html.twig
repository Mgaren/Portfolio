<form method="post" action="{{ path('projetPerso_delete', {'id': projetPerso.id}) }}" onsubmit="return confirm('Es tu sur de vouloir supprimer le projet?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projetPerso.id) }}">
    <button class="btn">Delete</button>
</form>
